/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'localhost',
      'digitalhippo-marketplace-production.up.railway.app',
    ],
  },
};

export default nextConfig;
